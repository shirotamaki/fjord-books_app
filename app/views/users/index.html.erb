<h1><%= User.model_name.human %>一覧</h1>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= user.email %>
    </li>
  <% end %>
</ul>

<%= paginate @users %>

<%= link_to t('views.common.back'), books_path %>
